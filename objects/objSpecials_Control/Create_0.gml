specials = objSettings_Tracker.specials_collected;
specials_count = array_length(specials);
specials[specials_count] = sptNone_Special;
specials_count++;
selected = 0;

menu_committed = false;

apply_fadein = true;

flash = 0;